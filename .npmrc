# Don't block `pnpm run/exec` when a workspace package's deps aren't installed.
# The frontend (apps/web) pulls Next.js/React, which need not be installed to run
# the backend package tests. CI installs everything with --frozen-lockfile.
verify-deps-before-run=false
